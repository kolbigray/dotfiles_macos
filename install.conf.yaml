- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
	



- create:
    - ~/downloads
    - ~/.vim/undo-history

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
