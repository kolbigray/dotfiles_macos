- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.vimrc:
    ~/.vim: 
    ~/.oh-my-zsh:
    ~/.viminfo:
    ~/.p10k.zsh:
    ~/.gitkraken:
    ~/.jupyter:
    ~/.npm:
    ~/.bash_profile:





#- create:
    

- shell:
  - [git submodule update --init --recursive, Installing submodules]
