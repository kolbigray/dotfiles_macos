- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.vimrc:
    ~/.vim: 
    ~/.oh-my-zsh:
    ~/.viminfo:
    ~/.p10k.zsh:
    ~/.gitkraken:
    ~/.jupyter:
    ~/.npm:
    



- create:
    - ~/downloads
    - ~/.vim/undo-history

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
